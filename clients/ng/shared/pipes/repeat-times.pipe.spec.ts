import { RepeatTimesPipe } from './repeat-times.pipe';

describe('RepeatTimesPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatTimesPipe();
    expect(pipe).toBeTruthy();
  });
});
